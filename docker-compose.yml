services:
  budgeteer:
    image: budgeteer:latest
    ports:
      - "80:3000"
    volumes:
      - ./data-volume:/app/apps/api/data
    env_file:
      - .env.docker
    environment:
      - NODE_ENV=production
    restart: unless-stopped
